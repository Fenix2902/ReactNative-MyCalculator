import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#3CF2FD",
    alignItems: "center",
    justifyContent: "center",
  },
  text: {
    color: "#fff",
    fontSize: 22,
    fontWeight: "bold",
    fontStyle: "italic",
  },

  tinputs: {
    padding: 5,
    textAlign: "center",
    backgroundColor: "white",
    borderRadius: 10,
  },
});

export const myimage = StyleSheet.create({
  images: {
    width: "100%",
    height: "100%",
  },
});
